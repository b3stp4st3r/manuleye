# 🔄 MANUL-EYE Refactoring Guide

## Новая модульная структура

Проект был реорганизован для лучшей поддерживаемости и расширяемости.

### 📁 Структура проекта

```
manuleye/
├── __init__.py                 # Главный инициализатор пакета
├── main.py                     # Старая монолитная версия (сохранена для совместимости)
├── main_new.py                 # Новая модульная версия
│
├── core/                       # Ядро фреймворка
│   ├── __init__.py
│   ├── config.py              # Константы и настройки
│   ├── utils.py               # Утилиты (validate_ip, select_file)
│   └── banner.py              # ASCII баннеры
│
├── modules/                    # Основные OSINT модули
│   ├── __init__.py
│   ├── lookup.py              # Поиск (username, email, phone, IP, domain)
│   ├── metadata.py            # Метаданные (EXIF, PDF, video, audio, office)
│   ├── network.py             # Сеть (port scan, traceroute, SSL, HTTP)
│   ├── security.py            # Безопасность (SQLMap, XSStrike, Instagram)
│   └── utilities.py           # Утилиты (paste, password gen, hash)
│
└── social/                     # Социальные сети (НОВОЕ!)
    ├── __init__.py
    ├── telegram.py            # Поиск в Telegram
    ├── tiktok.py              # Поиск в TikTok
    └── vk.py                  # Поиск в VK
```

## 🆕 Новые функции

### 1. Telegram Search (опция 23)
```python
from manuleye.social import telegram_search

telegram_search("username")
```

**Возможности:**
- Проверка существования username
- Извлечение имени и описания
- Определение типа (пользователь/канал)
- Ссылки на профиль (web и tg://)

### 2. TikTok Search (опция 24)
```python
from manuleye.social import tiktok_search

tiktok_search("username")
```

**Возможности:**
- Проверка существования профиля
- Извлечение nickname
- Подсчёт подписчиков (если доступно)
- Ссылка на профиль

### 3. VK Search (опция 25)
```python
from manuleye.social import vk_search

vk_search("username_or_id")
```

**Возможности:**
- Проверка существования профиля
- Извлечение User ID
- Извлечение имени
- Проверка наличия фото
- Инструкции по использованию VK API

## 🔧 Как использовать новую версию

### Вариант 1: Запуск новой модульной версии
```bash
python -m manuleye.main_new
```

### Вариант 2: Импорт отдельных модулей
```python
# Lookup модули
from manuleye.modules.lookup import username_hunt, phone_intel, ip_geolocate

# Social модули
from manuleye.social import telegram_search, tiktok_search, vk_search

# Использование
telegram_search("durov")
tiktok_search("username")
vk_search("id123456")
```

### Вариант 3: Старая версия (совместимость)
```bash
manuleye  # Использует старый main.py
```

## 📊 Сравнение версий

| Аспект | Старая версия | Новая версия |
|--------|---------------|--------------|
| Файлов | 1 (main.py) | 13+ модулей |
| Строк кода | ~1100 | Распределено |
| Поддержка | Сложная | Простая |
| Расширяемость | Низкая | Высокая |
| Тестируемость | Сложная | Простая |
| Социальные сети | Instagram | Instagram + Telegram + TikTok + VK |

## 🚀 Миграция

### Шаг 1: Обновите импорты
```python
# Старый способ
from manuleye.main import get_ip_info

# Новый способ
from manuleye.modules.lookup import ip_geolocate
```

### Шаг 2: Используйте новые функции
```python
# Telegram
from manuleye.social import telegram_search
telegram_search("username")

# TikTok
from manuleye.social import tiktok_search
tiktok_search("username")

# VK
from manuleye.social import vk_search
vk_search("username")
```

### Шаг 3: Обновите main.py (опционально)
Замените `main.py` на `main_new.py` для использования модульной версии:
```bash
mv manuleye/main.py manuleye/main_old.py
mv manuleye/main_new.py manuleye/main.py
```

## 🎯 Преимущества новой структуры

1. **Модульность** - каждый модуль отвечает за свою область
2. **Расширяемость** - легко добавлять новые функции
3. **Тестируемость** - можно тестировать модули отдельно
4. **Читаемость** - код организован логически
5. **Поддержка** - легче находить и исправлять баги
6. **Новые функции** - Telegram, TikTok, VK поиск

## 📝 Добавление новых модулей

### Пример: Добавление поиска в Discord

1. Создайте файл `manuleye/social/discord.py`:
```python
from manuleye.core import console

def discord_search(username):
    console.print(f"[cyan]Searching Discord for: {username}[/cyan]")
    # Ваш код здесь
```

2. Обновите `manuleye/social/__init__.py`:
```python
from .discord import discord_search
__all__ = [..., 'discord_search']
```

3. Добавьте в меню `main_new.py`:
```python
elif choice == "28":
    username = Prompt.ask("Discord username")
    discord_search(username)
```

## 🔍 API ключи для расширенного поиска

### Telegram API
- Получить: https://my.telegram.org
- Библиотеки: `telethon`, `pyrogram`

### TikTok API
- Официальный: https://developers.tiktok.com/
- Альтернатива: RapidAPI

### VK API
- Получить: https://vk.com/dev
- Документация: https://dev.vk.com/method

## 📚 Дополнительные ресурсы

- [Документация Telegram Bot API](https://core.telegram.org/bots/api)
- [TikTok Research API](https://developers.tiktok.com/products/research-api/)
- [VK API Documentation](https://dev.vk.com/reference)

## ⚠️ Важные замечания

1. **Совместимость**: Старая версия `main.py` сохранена для обратной совместимости
2. **Постепенная миграция**: Функции будут постепенно переноситься в модули
3. **API ограничения**: Некоторые функции требуют API ключей
4. **Rate limiting**: Соблюдайте ограничения API социальных сетей

## 🎉 Что дальше?

1. Перенести все функции из `main.py` в модули
2. Добавить больше социальных сетей (Discord, Reddit, LinkedIn)
3. Реализовать API интеграции
4. Добавить unit тесты
5. Создать CLI с argparse для автоматизации

---

**Версия:** 1.1.0 (модульная)  
**Дата:** 15 февраля 2026
